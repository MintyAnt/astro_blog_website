
import React from 'react';
import Section from '../common/Section';

const Contact = () => {
    const styles = {
        text: {
            width: '60%',
            height: '100%',
            float: 'left',
        },
    };

    return (
        <Section height="150px">
            Contact
        </Section>
    );
};

export default Contact;
